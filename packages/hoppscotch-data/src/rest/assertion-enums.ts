export enum assertionTextTypes {
  HTTP_STATUS_CODE = "Http Status Code",
  BODY = "Body",
  HEADERS = "Headers",
}

export enum assertionTextConditions {
  CONTAINS = "Contains",
  NOT_CONTAIN = "Doesn't Contain",
  EQUALS = "Equals",
  STARTS_WITH = "Starts With",
}

export enum assertionJsonPathConditions {
  EQUALS = "Equals",
  NOT_EQUALS = "Not Equals",
}

